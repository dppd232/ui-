#  AI 병렬 검사 통합 모니터링 시스템

여러 클라이언트가 접속하여 실시간으로 검사 로그와 결과를 공유하는 대시보드입니다.

##  실행 방법
1. 코드를 다운로드합니다. (오른쪽 초록색 Code 버튼 -> Download ZIP)
2. 터미널에서 라이브러리를 설치합니다: `pip install -r requirements.txt`
3. 서버를 실행합니다: `python app.py`
4. 브라우저에서 접속합니다: `http://localhost:5000`
